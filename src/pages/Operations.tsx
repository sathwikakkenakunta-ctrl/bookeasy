import {useMemo,useState} from 'react';import {Bar,BarChart,CartesianGrid,Cell,Legend,Pie,PieChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';import {ArrowDownToLine,CalendarDays,Check,ChevronLeft,ChevronRight,Clock3,Edit3,Ellipsis,Filter,Grid2X2,LayoutList,Mail,MoreHorizontal,Plus,Search,SlidersHorizontal,Star,Trash2,UserRound,Users2} from 'lucide-react';import {useLocation} from 'react-router-dom';import {toast} from 'sonner';import {Avatar,Badge,Button,Empty,Modal} from '../components/UI';import {revenue} from '../data';import {useAppStore} from '../store';import type {Service,Status} from '../types';
const PageHead=({eyebrow,title,text,children}:{eyebrow:string;title:string;text:string;children?:React.ReactNode})=><div className="page-head"><div><span className="kicker">{eyebrow}</span><h1>{title}</h1><p>{text}</p></div><div>{children}</div></div>;
export default function Operations(){const key=useLocation().pathname.split('/').pop()!;if(key==='calendar')return <Calendar/>;if(key==='appointments')return <Appointments/>;if(key==='customers')return <Customers/>;if(key==='services')return <Services/>;if(key==='team')return <Team/>;if(key==='analytics')return <Analytics/>;if(key==='reports')return <Reports/>;return <Settings/>}
function Calendar(){const {bookings}=useAppStore();const [view,setView]=useState('Week');const hours=['9 AM','10 AM','11 AM','12 PM','1 PM','2 PM','3 PM','4 PM','5 PM'];return <div className="page-enter"><PageHead eyebrow="SCHEDULE" title="Calendar" text="Your team’s time, beautifully organized."><Button><Plus/> New booking</Button></PageHead><section className="panel calendar-panel"><header className="calendar-tools"><div><button className="icon-btn"><ChevronLeft/></button><button className="icon-btn"><ChevronRight/></button><Button variant="secondary">Today</Button><h2>June 17 – 23, 2026</h2></div><div className="segmented">{['Month','Week','Day','Agenda'].map(x=><button className={view===x?'active':''} onClick={()=>setView(x)} key={x}>{x}</button>)}</div></header>{view==='Agenda'?<div className="agenda-list">{bookings.map(b=><article key={b.id}><span>{b.date}<b>{b.time}</b></span><i style={{background:'var(--brand)'}}/><Avatar name={b.customer}/><div><b>{b.service}</b><small>{b.customer} with {b.staff}</small></div><Badge tone={b.status==='Confirmed'?'success':'warning'}>{b.status}</Badge></article>)}</div>:<div className="week-calendar"><div className="week-corner"/><div className="week-days">{[['MON','17'],['TUE','18'],['WED','19'],['THU','20'],['FRI','21'],['SAT','22'],['SUN','23']].map((x,i)=><div className={i===4?'today':''} key={x[0]}><span>{x[0]}</span><b>{x[1]}</b></div>)}</div><div className="time-axis">{hours.map(x=><span key={x}>{x}</span>)}</div><div className="calendar-cells">{hours.flatMap((_,h)=>Array.from({length:7},(_,d)=><div key={`${h}-${d}`}/>))}{bookings.slice(0,6).map((b,i)=><article className={'cal-event event-'+i} key={b.id}><b>{b.service}</b><span>{b.time} · {b.customer}</span><small>{b.staff}</small></article>)}</div></div>}</section></div>}
function Appointments(){const {bookings,updateBooking}=useAppStore();const [q,setQ]=useState('');const [status,setStatus]=useState('All');const filtered=bookings.filter(b=>(status==='All'||b.status===status)&&(b.customer+b.service+b.id).toLowerCase().includes(q.toLowerCase()));const change=(id:string,s:Status)=>{updateBooking(id,{status:s});toast.success(`Appointment marked ${s.toLowerCase()}`)};return <div className="page-enter"><PageHead eyebrow="BOOKINGS" title="Appointments" text="Manage every client visit from one place."><Button><Plus/> New booking</Button></PageHead><section className="panel table-panel"><div className="table-tools"><label><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search appointments..."/></label><div className="segmented">{['All','Confirmed','Pending','Completed'].map(x=><button className={status===x?'active':''} onClick={()=>setStatus(x)} key={x}>{x}</button>)}</div><Button variant="secondary"><Filter/> Filters</Button></div><div className="data-table"><table><thead><tr><th>Client</th><th>Appointment</th><th>Date & time</th><th>Professional</th><th>Status</th><th>Payment</th><th/></tr></thead><tbody>{filtered.map(b=><tr key={b.id}><td><div className="person-cell"><Avatar name={b.customer}/><span><b>{b.customer}</b><small>{b.id}</small></span></div></td><td><b>{b.service}</b><small>{b.duration} min · ${b.price}</small></td><td><b>{b.date}</b><small>{b.time}</small></td><td>{b.staff}</td><td><Badge tone={b.status==='Confirmed'?'success':b.status==='Pending'?'warning':b.status==='Cancelled'?'danger':'neutral'}>{b.status}</Badge></td><td><Badge tone={b.payment==='Paid'?'success':'warning'}>{b.payment}</Badge></td><td><div className="row-actions"><button onClick={()=>change(b.id,'Completed')} title="Complete"><Check/></button><button onClick={()=>change(b.id,'Cancelled')} title="Cancel"><Trash2/></button></div></td></tr>)}</tbody></table>{!filtered.length&&<Empty icon={<CalendarDays/>} title="No appointments found" text="Try changing your search or filters."/>}</div></section></div>}
function Customers(){const {customers}=useAppStore();const [q,setQ]=useState('');const list=customers.filter(c=>(c.name+c.email+c.favorite).toLowerCase().includes(q.toLowerCase()));return <div className="page-enter"><PageHead eyebrow="RELATIONSHIPS" title="Customers" text="Every detail that helps you make clients feel known."><Button><Plus/> Add customer</Button></PageHead><div className="toolbar"><label><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search by name, email, or service..."/></label><Button variant="secondary"><SlidersHorizontal/> Filters</Button><div className="view-buttons"><button className="active"><Grid2X2/></button><button><LayoutList/></button></div></div><div className="customer-grid">{list.map(c=><article className="customer-card" key={c.id}><button><Ellipsis/></button><Avatar name={c.name} color={['#a46157','#527381','#8f6a82','#5d7d6d','#aa7d57'][customers.indexOf(c)]} size="lg"/><h3>{c.name}</h3><a><Mail/> {c.email}</a><div className="customer-stats"><span><b>{c.visits}</b><small>Visits</small></span><span><b>${c.spent}</b><small>Spent</small></span></div><div className="favorite"><Star/><span><small>FAVORITE SERVICE</small><b>{c.favorite}</b></span></div><footer><span>Last visit {c.lastVisit}</span><button>View profile <ChevronRight/></button></footer></article>)}</div></div>}
function Services(){const {services,addService,deleteService}=useAppStore();const [open,setOpen]=useState(false);const [form,setForm]=useState({name:'',category:'Wellness',duration:'60',price:'95',description:''});const save=(e:React.FormEvent)=>{e.preventDefault();const item:Service={id:crypto.randomUUID(),name:form.name,category:form.category,duration:+form.duration,price:+form.price,description:form.description,color:'#8a5b72',active:true};addService(item);setOpen(false);toast.success('Service created')};return <div className="page-enter"><PageHead eyebrow="YOUR OFFERINGS" title="Services" text="Shape a menu that feels distinctly yours."><Button onClick={()=>setOpen(true)}><Plus/> Create service</Button></PageHead><div className="service-admin-grid">{services.map(s=><article key={s.id}><div className="service-color" style={{background:`linear-gradient(135deg,${s.color},color-mix(in srgb,${s.color} 55%,#fff))`}}><span>{s.category}</span>{s.popular&&<Badge>POPULAR</Badge>}</div><div className="service-body"><div><h3>{s.name}</h3><button><MoreHorizontal/></button></div><p>{s.description}</p><div className="service-meta"><span><Clock3/> {s.duration} min</span><b>${s.price}</b></div><footer><Badge tone="success">● Active</Badge><div><button><Edit3/></button><button onClick={()=>{deleteService(s.id);toast.success('Service removed')}}><Trash2/></button></div></footer></div></article>)}</div><Modal open={open} onClose={()=>setOpen(false)} title="Create a new service"><form className="modal-form" onSubmit={save}><label>Service name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="e.g. Glow Ritual"/></label><div className="form-row"><label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option>Wellness</option><option>Beauty</option><option>Hair</option><option>Skincare</option></select></label><label>Duration<input type="number" value={form.duration} onChange={e=>setForm({...form,duration:e.target.value})}/></label><label>Price ($)<input type="number" value={form.price} onChange={e=>setForm({...form,price:e.target.value})}/></label></div><label>Description<textarea rows={3} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><div className="modal-actions"><Button type="button" variant="ghost" onClick={()=>setOpen(false)}>Cancel</Button><Button type="submit">Create service</Button></div></form></Modal></div>}
function Team(){const {staff}=useAppStore();return <div className="page-enter"><PageHead eyebrow="YOUR PEOPLE" title="Team" text="Availability, performance, and working hours in harmony."><Button><Plus/> Invite team member</Button></PageHead><div className="team-admin-grid">{staff.map(s=><article key={s.id}><header><Avatar name={s.name} color={s.color} size="lg"/><div><h3>{s.name}</h3><p>{s.role}</p></div><button><MoreHorizontal/></button></header><div className="rating">★ {s.rating} <span>· {s.bookings} bookings</span></div><div className="performance"><span>Utilization <b>{s.utilization}%</b></span><i><b style={{width:`${s.utilization}%`}}/></i></div><footer><Badge tone={s.workingToday?'success':'neutral'}>{s.workingToday?'Available today':'Off today'}</Badge><button>View schedule</button></footer></article>)}</div></div>}
function Analytics(){const {services}=useAppStore();const pie=services.slice(0,5).map((s,i)=>({name:s.name,value:[34,26,18,13,9][i],color:s.color}));return <div className="page-enter"><PageHead eyebrow="BUSINESS HEALTH" title="Analytics" text="Clear signals for thoughtful decisions."><select className="select-control"><option>Last 6 months</option></select></PageHead><div className="analytics-stats">{[['Total revenue','$64,940','18.4%'],['Bookings','750','12.8%'],['Avg. value','$86.59','4.2%'],['Cancellation rate','3.8%','-1.1%']].map(x=><article key={x[0]}><span>{x[0]}</span><strong>{x[1]}</strong><Badge tone="success">↗ {x[2]}</Badge></article>)}</div><div className="analytics-grid"><section className="panel"><header><div><h2>Revenue & bookings</h2><p>Monthly performance</p></div></header><div className="large-chart"><ResponsiveContainer><BarChart data={revenue}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="m"/><YAxis yAxisId="a"/><YAxis yAxisId="b" orientation="right"/><Tooltip/><Legend/><Bar yAxisId="a" dataKey="v" name="Revenue" fill="#713c56" radius={[6,6,0,0]}/><Bar yAxisId="b" dataKey="b" name="Bookings" fill="#dfa08c" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div></section><section className="panel"><header><div><h2>Popular services</h2><p>Share of all bookings</p></div></header><div className="pie-wrap"><ResponsiveContainer><PieChart><Pie data={pie} dataKey="value" innerRadius={65} outerRadius={95} paddingAngle={3}>{pie.map(x=><Cell fill={x.color} key={x.name}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer><strong>750<small>BOOKINGS</small></strong></div><div className="pie-legend">{pie.map(x=><span key={x.name}><i style={{background:x.color}}/>{x.name}<b>{x.value}%</b></span>)}</div></section></div></div>}
function Reports(){const reports=[['Booking report','All appointment activity, status, and booking sources.'],['Revenue report','Payments, refunds, tax, tips, and service revenue.'],['Customer report','Retention, lifetime value, and visit frequency.'],['Service report','Performance, demand, and average booking value.']];return <div className="page-enter"><PageHead eyebrow="EXPORT & REVIEW" title="Reports" text="Detailed records, ready whenever you need them."><Button variant="secondary"><ArrowDownToLine/> Export all</Button></PageHead><div className="report-grid">{reports.map((x,i)=><article key={x[0]}><span className={'report-icon r'+i}><UserRound/></span><div><h3>{x[0]}</h3><p>{x[1]}</p><small>Updated moments ago</small></div><Button variant="secondary" onClick={()=>toast.success(`${x[0]} exported`)}><ArrowDownToLine/> Export CSV</Button></article>)}</div></div>}
function Settings(){const {theme,setTheme,resetDemo}=useAppStore();return <div className="page-enter"><PageHead eyebrow="WORKSPACE" title="Settings" text="Make BookEasy feel like your business."/><div className="settings-layout"><nav>{['Business profile','Branding & theme','Working hours','Notifications','Booking rules','Team permissions','Security'].map((x,i)=><button className={i===1?'active':''} key={x}>{x}</button>)}</nav><section className="panel settings-panel"><h2>Branding & theme</h2><p>Choose how your workspace and public booking page feel.</p><hr/><label>Business name<input defaultValue="Maison Amélie"/></label><label>Brand color<div className="color-row">{['#54243c','#d57d65','#668277','#657d91','#9a7550'].map(x=><button key={x} style={{background:x}} className={x==='#54243c'?'selected':''}/>)}</div></label><label>Theme<div className="theme-options">{['light','dark','system'].map(x=><button className={theme===x?'active':''} onClick={()=>setTheme(x as 'light'|'dark'|'system')} key={x}><span className={x}/><b>{x[0].toUpperCase()+x.slice(1)}</b></button>)}</div></label><div className="settings-actions"><Button variant="danger" onClick={()=>{resetDemo();toast.success('Demo data restored')}}>Reset demo data</Button><Button onClick={()=>toast.success('Settings saved')}>Save changes</Button></div></section></div></div>}
